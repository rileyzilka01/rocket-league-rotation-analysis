                #l1Penalty = self.l1Ratio * np.sign(w) * self.l1Strength
                #l2Penalty = (1 - self.l1Ratio) * w * self.l2Strength
                #dW += l1Penalty + l2Penalty
#GRADIENT



            #l1_regularization = self.l1Strength * np.sum(np.abs(w))
            #l2_regularization = self.l1Strength * np.sum(np.square(w))
            #loss = mse + l1_regularization + l2_regularization

#LOSS